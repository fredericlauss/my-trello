<form method="post" action="{{ path('app_columns_delete', {'id': column.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ column.id) }}">
    <button class="btn">Delete</button>
</form>
